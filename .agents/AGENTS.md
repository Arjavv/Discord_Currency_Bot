# Developer Guidelines

## Git Branching & Deployment Strategy
- **Never push directly to the `main` branch** for fixes, features, or hotfixes.
- All development and bug fixing must be done on dedicated feature branches (e.g., `dev`, `feature/*`, `bugfix/*`).
- Stage changes and verify them in a testing/staging environment before merging them into `main` via Pull Requests.
